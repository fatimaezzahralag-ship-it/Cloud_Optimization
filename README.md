# Cloud Optimization

Projet de démonstration pour l'optimisation cloud (backend Laravel + frontend Vite/React).

Structure principale:
- `backend/agentic-backend` — API Laravel
- `frontend/` — application React + Vite

Pour contribuer:
1. Ajoutez votre clé SSH GitHub (voir `ssh_keys` section dans les instructions). 
2. Clonez le dépôt en SSH: `git@github.com:fatimaezzahralag-ship-it/Cloud_Optimization.git`.

Licence: MIT