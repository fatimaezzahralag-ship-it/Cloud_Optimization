# Cloud Optimization — Frontend

This is a minimal React + Vite scaffold for the Cloud Optimization frontend.

Quick start:

1. Open a terminal inside `frontend`:

```bash
cd frontend
```

2. Install dependencies:

```bash
npm install
```

3. Start the dev server:

```bash
npm run dev
```

The dev server runs on `http://localhost:3000` by default.
